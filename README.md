# airflow_data_pipeline_coursework
In this coursework, build a complete data pipeline that runs on a local machine using Apache Airflow deployed with Docker. The pipeline should extract data from a public API or through web scraping, store the collected data in a relational database, and generate visual insights from the stored data.
